# 分布式与中间件领域索引

| 锚点字 | 知识点 | 口诀 | 类型 |
|--------|--------|------|------|
| 延 | RabbitMQ怎么实现延迟队列 | 延T死，两法延 | 因果型 |
| 轻 | 为什么RocketMQ不使用Zookeeper | 轻无状，三原因 | 因果型 |
| 死 | RabbitMQ中消息什么时候会进入死信交换机 | 死拒过满，三条件 | 列举型 |
| 备 | RabbitMQ中无法路由的消息会去到哪里 | 备退回，两去处 | 因果型 |
| 弃 | Kafka为什么要抛弃Zookeeper | 弃耦扩，三原因 | 因果型 |
| 管 | Kafka中Zookeeper的作用 | 管选主，三作用 | 列举型 |
| 事 | Kafka中关于事务消息的实现 | 事两标，幂提交 | 因果型 |
| 半 | RocketMQ中关于事务消息的实现 | 半查提，四步走 | 流程型 |
| 缺 | RocketMQ的事务消息有什么缺点 | 缺耦回，三缺点 | 因果型 |
| 解 | 为什么需要消息队列 | 解削异，三需求 | 因果型 |
| 模 | 消息队列的模型有哪些 | 模点对，两模型 | 对比型 |
| 配 | 什么是配置中心 | 配集热，三中心 | 列举型 |
| 分 | 什么情况下需要使用分布式事务 | 分XA，四方案 | 列举型 |
| 拆 | 分布式和微服务有什么区别 | 拆独治，四区别 | 对比型 |
| 分 | 什么是 Seata | 分 AT，四模式 | 因果型 |
| 红 | 分布式锁一般都怎样实现 | 红数ZK，三方案 | 列举型 |
| 通 | HTTP 与 RPC 之间的区别 | 通协效，五区别 | 对比型 |
| 代 | 让你设计一个 RPC 框架，怎么设计 | 代编网传，五模块 | 流程型 |
| 桶 | 什么是限流？限流算法有哪些？怎么实现的 | 桶漏窗，三算法 | 列举型 |
| 均 | 负载均衡算法有哪些 | 均权最，六算法 | 列举型 |
| 降 | 什么是服务降级 | 降替默，三策略 | 因果型 |
| 崩 | 什么是服务雪崩 | 崩链级，三解法 | 因果型 |
| 幂 | 消息队列如何处理重复消息（保证消息的幂等性） | 幂唯一，四方案 | 因果型 |
| 序 | 消息队列如何保证消息的有序性（顺序性） | 序单分，三方案 | 因果型 |
| 堆 | 消息队列如何处理消息堆积 | 堆扩限，五解法 | 因果型 |
| 断 | 什么是服务熔断 | 断降恢，三状态 | 因果型 |
| 落 | 消息队列如何保证消息不丢失 | 落异确，三阶段 | 因果型 |
| 拉 | 消息队列设计成推消息还是拉消息？推拉模式的优缺点 | 拉控推实，两模式 | 对比型 |
| 网 | Dubbo 和 Spring Cloud Gateway 有什么区别 | 网关调，两定位 | 对比型 |
| 关 | 说说什么是 API 网关它有什么作用 | 关路限，五作用 | 列举型 |

---

## 知识链路图

### 消息队列链路
```
延T死，两法延（RabbitMQ延迟队列）
        ↓
死拒过满，三条件（RabbitMQ死信交换机）
        ↓
轻无状，三原因（RocketMQ NameServer）
```


### Kafka链路
```
管选主，三作用（Kafka中Zookeeper作用）
        ↓
弃耦扩，三原因（Kafka抛弃Zookeeper）
        ↓
事两标，幂提交（Kafka事务消息）
```

### RocketMQ链路
```
轻无状，三原因（RocketMQ NameServer）
        ↓
半查提，四步走（RocketMQ事务消息）
        ↓
缺耦回，三缺点（RocketMQ事务消息缺点）
```

### RabbitMQ链路
```
延T死，两法延（RabbitMQ延迟队列）
        ↓
死拒过满，三条件（RabbitMQ死信交换机）
        ↓
备退回，两去处（RabbitMQ无法路由消息）
```

### 消息队列基础链路
```
解削异，三需求（为什么需要消息队列）
        ↓
模点对，两模型（消息队列模型）
```


### 微服务基础设施链路
```
配集热，三中心（配置中心）
        ↓
分XA，四方案（分布式事务）
```


### 微服务网关链路
```
关路限，五作用（API网关）
        ↓
网关调，两定位（Dubbo vs Gateway）
        ↓
配集热，三中心（配置中心）
        ↓
分XA，四方案（分布式事务）
```


### 消息队列完整链路
```
落异确，三阶段（保证消息不丢失）
        ↓
幂唯一，四方案（处理重复消息）
        ↓
序单分，三方案（保证有序性）
        ↓
拉控推实，两模式（推拉模式）
        ↓
堆扩限，五解法（处理消息堆积）
        ↓
死拒过满，三条件（死信交换机）
```

### 微服务稳定性链路
```
断降恢，三状态（服务熔断）
        ↓
关路限，五作用（API网关）
        ↓
网关调，两定位（Dubbo vs Gateway）
```


### 微服务通信链路
```
通协效，五区别（HTTP vs RPC）
        ↓
代编网传，五模块（RPC框架设计）
        ↓
网关调，两定位（Dubbo vs Gateway）
        ↓
均权最，六算法（负载均衡）
```

### 微服务稳定性链路
```
崩链级，三解法（服务雪崩）
        ↓
断降恢，三状态（服务熔断）
        ↓
降替默，三策略（服务降级）
        ↓
桶漏窗，三算法（限流）
        ↓
关路限，五作用（API网关）
```

### 分布式锁链路
```
红数ZK，三方案（分布式锁实现）
```


### 分布式事务与微服务链路
```
拆独治，四区别（分布式 vs 微服务）
        ↓
代编网传，五模块（RPC框架设计）
        ↓
通协效，五区别（HTTP vs RPC）
        ↓
分AT，四模式（Seata）
        ↓
分XA，四方案（分布式事务方案）
        ↓
均权最，六算法（负载均衡）
        ↓
桶漏窗，三算法（限流）
        ↓
崩链级，三解法（服务雪崩）
        ↓
断降恢，三状态（服务熔断）
        ↓
降替默，三策略（服务降级）
        ↓
关路限，五作用（API网关）
```

### 分布式锁链路
```
红数ZK，三方案（分布式锁实现）
```

### 复习用法
考前10分钟复习流程：
1. 先看 速记卡片表格（30秒）
2. 再看 知识链路图（1分钟）——顺着链路默念口诀
3. 最后看 总索引 _INDEX.md（30秒）——跨领域联想
