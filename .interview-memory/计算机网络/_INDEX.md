# 计算机网络领域索引

| 锚点字 | 知识点 | 口诀 | 类型 |
|--------|--------|------|------|
| 握 | 说说TCP的三次握手 | 握同确，我同意 | 流程型 |
| 挥 | 说说TCP的四次挥手 | 挥等确，我先走 | 流程型 |
| 等 | 为什么 TCP 挥手需要有 TIME_WAIT 状态 | 等两MSL，旧包死透 | 因果型 |
| 丢 | TCP 超时重传机制是为了解决什么问题 | 丢包超时，三招补网 | 因果型 |
| 流 | TCP 滑动窗口的作用是什么 | 窗口滑动，流控拥塞 | 因果型 |
| 传 | TCP IP 四层模型是什么 | 传网链应，四层上楼 | 列举型 |
| 物 | OSI 七层模型是什么 | 物数网传会表应 | 列举型 |
| 状 | Cookie、Session、Token 之间有什么区别 | 状存签三分天下 | 对比型 |

---

## 知识链路图

### TCP连接链路
```
握同确，我同意（三次握手）
        ↓
连保稳，断不管（TCP vs UDP）
        ↓
挥等确，我先走（四次挥手）
```


### TCP传输机制链路
```
传网链应，四层上楼（TCP/IP四层模型）
        ↓
物数网传会表应（OSI七层模型）
        ↓
握同确，我同意（三次握手）
        ↓
挥等确，我先走（四次挥手）
        ↓
等两MSL，旧包死透（TIME_WAIT）
        ↓
丢包超时，三招补网（超时重传）
        ↓
窗口滑动，流控拥塞（滑动窗口）
```

### 网络认证链路
```
状存签三分天下（Cookie/Session/Token）
```


### 网页访问全流程链路
```
输网TCP，HT渲（输入网址到网页显示）
        ↓
握同确，我同意（三次握手）
        ↓
我问你要，你给我证，我发钥匙，一起开门（HTTPS握手）
        ↓
挥等确，我先走（四次挥手）
        ↓
等两MSL，旧包死透（TIME_WAIT）
        ↓
丢包超时，三招补网（超时重传）
        ↓
窗口滑动，流控拥塞（滑动窗口）
```


### HTTP演进链路
```
传网链应，四层上楼（TCP/IP四层模型）
        ↓
握同确，我同意（三次握手）
        ↓
四态序，三要素（TCP连接）
        ↓
二头多，五特性（HTTP 1.0 vs 2.0）
```


### 网络协议基础链路
```
传网链应，四层上楼（TCP/IP四层模型）
        ↓
靠序流，三问题（TCP解决什么问题）
        ↓
握同确，我同意（三次握手）
        ↓
四态序，三要素（TCP连接）
        ↓
等两MSL，旧包死透（TIME_WAIT）
        ↓
证密端，四区别（HTTP vs HTTPS）
        ↓
二头多，五特性（HTTP 1.0 vs 2.0）
        ↓
三快连，五区别（HTTP 2.0 vs 3.0）
```

### 复习用法
考前10分钟复习流程：
1. 先看 速记卡片表格（30秒）
2. 再看 知识链路图（1分钟）——顺着链路默念口诀
3. 最后看 总索引 _INDEX.md（30秒）——跨领域联想
