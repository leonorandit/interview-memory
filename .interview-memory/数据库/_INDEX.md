# 数据库领域索引

| 锚点字 | 知识点 | 口诀 | 类型 |
|--------|--------|------|------|
| 矮 | MySQL索引为什么用B+树 | 叶矮顺，散红黑高 | 因果型 |
| 左 | MySQL索引的最左前缀匹配原则 | 左到右，不断点 | 因果型 |
| 读 | 数据库的脏读、不可重复读和幻读 | 读重复，幻脏三 | 列举型 |
| 引 | MySQL的存储引擎有哪些 | 引My聚，两主引 | 对比型 |
| 盖 | MySQL的覆盖索引 | 盖不回，查索引 | 因果型 |
| 类 | MySQL的索引类型有哪些 | 类聚联全，四索引 | 列举型 |
| 下 | MySQL的索引下推 | 下先滤，少回表 | 因果型 |
| 聚 | MySQL InnoDB引擎中的聚簇索引和非聚簇索引 | 聚数一，非聚指 | 对比型 |
| 回 | MySQL中的回表 | 回主查，二步走 | 因果型 |
| 查 | MySQL中使用索引一定有效吗 | 查Explain，四看效 | 因果型 |
| 多 | MySQL中的索引数量是否越多越好 | 多写慢，三代价 | 因果型 |
| 查 | MySQL的B+树查询全过程 | 查根叶，三步骤 | 流程型 |
| 叶 | 为什么MySQL选择使用B+树 | 叶矮顺，散红黑高 | 因果型 |
| 亿 | MySQL三层B+树能存多少数据 | 亿键叶，三层存 | 因果型 |
| 执 | 详细描述一条SQL语句在MySQL中的执行过程 | 执解优执，四步走 | 流程型 |
| redo | MySQL是如何实现事务的 | redo回锁，三机制 | 因果型 |
| 两 | MySQL事务的二阶段提交是什么 | 两redo bin，保一致 | 因果型 |
| 久 | MySQL中长事务可能会导致哪些问题 | 久锁胀，三问题 | 因果型 |
| 版 | MySQL中的MVCC是什么 | 版读不加，三组件 | 因果型 |
| 级 | MySQL中的事务隔离级别有哪些 | 级读提交，幻可串 | 列举型 |
| 默 | MySQL默认的事务隔离级别是什么 | 默可防，幻主从 | 因果型 |
| 锁 | MySQL中有哪些锁类型 | 锁表行，共排两 | 列举型 |
| 悲 | MySQL的乐观锁和悲观锁 | 悲先加，乐后比 | 对比型 |
| 等 | MySQL中如果发生死锁应该如何解决 | 等超顺，四解法 | 因果型 |
| 全 | MySQL中count(*)、count(1)和count(字段名)的区别 | 全一行，空不算 | 对比型 |
| 慢 | MySQL中如何进行SQL调优 | 慢索执，四步调 | 流程型 |
| 型 | 如何使用MySQL的EXPLAIN语句 | 型键行，五看板 | 列举型 |
| 槽 | Redis集群的实现原理 | 槽分迁，三原理 | 因果型 |
| 裂 | Redis集群会出现脑裂问题吗 | 裂少票，两方案 | 因果型 |
| 锁 | Redis中如何实现分布式锁 | 锁红看，Lua保 | 因果型 |
| 深 | MySQL中如何解决深度分页的问题 | 深子延，三解法 | 因果型 |
| 失 | Redis实现分布式锁时可能遇到的问题 | 失误可，三问题 | 列举型 |
| 从 | MySQL的主从同步机制 | 从dump推，三线程 | 因果型 |
| 看 | Redisson分布式锁的原理 | 看Lua，三原理 | 因果型 |
| 排 | 如何使用Redis快速实现排行榜 | 排有Z，三命令 | 因果型 |
| 一 | Redis中如何保证缓存与数据库的数据一致性 | 一删延，四策略 | 因果型 |
| 单 | Redis为什么这么快 | 单内模，五原因 | 因果型 |
| 布 | 如何使用Redis快速实现布隆过滤器 | 布哈希，可错无 | 因果型 |
| 延 | 如何处理MySQL的主从同步延迟 | 延强并，五解法 | 因果型 |
| 线 | 为什么Redis设计为单线程 | 线令简，6加IO | 对比型 |
| 客 | 你在项目中使用的 Redis 客户端是什么 | 客J红，三选型 | 列举型 |
| 五 | Redis 中常见的数据类型有哪些 | 五字列，五类型 | 列举型 |
| 跳 | Redis 中跳表的实现原理是什么 | 跳多速，概率升 | 因果型 |
| 慢 | Redis 性能瓶颈时如何处理 | 慢命分，五优化 | 因果型 |


### MySQL索引原理链路
```
叶矮顺，散红黑高（为什么用B+树）
        ↓
查根叶，三步骤（B+树查询全过程）
        ↓
类聚联全，四索引（索引类型）
        ↓
聚数一，非聚指（聚簇vs非聚簇索引）
        ↓
盖不回，查索引（覆盖索引）
        ↓
回主查，二步走（回表）
        ↓
下先滤，少回表（索引下推）
        ↓
左到右，不断点（最左前缀）
        ↓
查Explain，四看效（索引有效性排查）
        ↓
多写慢，三代价（索引数量）
```

### 事务与存储引擎链路
```
引My聚，两主引（存储引擎对比）
        ↓
读重复，幻脏三（脏读/不可重复读/幻读）
```


### MySQL事务与并发链路
```
redo回锁，三机制（事务实现）
        ↓
两redo bin，保一致（二阶段提交）
        ↓
级读提交，幻可串（隔离级别）
        ↓
默可防，幻主从（默认RR）
        ↓
版读不加，三组件（MVCC）
        ↓
锁表行，共排两（锁类型）
        ↓
久锁胀，三问题（长事务问题）
```

### MySQL执行与容量链路
```
执解优执，四步走（SQL执行过程）
        ↓
亿键叶，三层存（B+树容量）
```


### MySQL锁与调优链路
```
悲先加，乐后比（乐观锁vs悲观锁）
        ↓
锁表行，共排两（锁类型）
        ↓
等超顺，四解法（死锁解决）
        ↓
慢索执，四步调（SQL调优）
        ↓
型键行，五看板（EXPLAIN分析）
        ↓
深子延，三解法（深度分页）
        ↓
全一行，空不算（count区别）
```

### Redis集群与分布式锁链路
```
槽分迁，三原理（Redis集群原理）
        ↓
裂少票，两方案（Redis脑裂）
        ↓
锁红看，Lua保（Redis分布式锁）
        ↓
失误可，三问题（分布式锁问题）
```


### Redis性能与特性链路
```
单内模，五原因（Redis为什么这么快）
        ↓
线令简，6加IO（单线程vs多线程）
        ↓
排有Z，三命令（排行榜）
        ↓
布哈希，可错无（布隆过滤器）
```

### Redis分布式链路
```
锁红看，Lua保（Redis分布式锁）
        ↓
看Lua，三原理（Redisson分布式锁）
        ↓
失误可，三问题（分布式锁问题）
        ↓
一删延，四策略（缓存一致性）
        ↓
槽分迁，三原理（Redis集群）
        ↓
裂少票，两方案（Redis脑裂）
```

### MySQL主从链路
```
从dump推，三线程（主从同步机制）
        ↓
延强并，五解法（主从同步延迟）
```


### Redis数据结构与客户端链路
```
客J红，三选型（Redis客户端）
        ↓
五字列，五类型（Redis数据类型）
        ↓
跳多速，概率升（Redis跳表）
        ↓
慢命分，五优化（Redis性能瓶颈）
```


### Redis横向对比链路
```
客J红，三选型（Redis客户端）
        ↓
多持持，五区别（Redis vs Memcached）
        ↓
五字列，五类型（Redis数据类型）
        ↓
对键值，两结构（Redis Hash）
        ↓
批弱乐，三命令（Redis事务）
```

### MySQL扩展链路
```
从dump推，三线程（主从同步）
        ↓
延强并，五解法（主从延迟）
        ↓
拆垂水，两维四策（分库分表）
```


### Redis内存管理链路
```
懒定随，三策略（过期删除策略）
        ↓
淘近LFU，八策略（内存淘汰策略）
        ↓
单内模，五原因（Redis为什么快）
```

### MySQL大表查询链路
```
流缓升，三因素（select大表内存飙升）
        ↓
深子延，三解法（深度分页）
```


### Redis性能与优化链路
```
缓锁分，六场景（Redis应用场景）
        ↓
大拆删，四解法（Big Key问题）
        ↓
懒定随，三策略（过期删除策略）
        ↓
淘近LFU，八策略（内存淘汰策略）
        ↓
批一发，三优势（Pipeline）
        ↓
原评调，三用途（Lua脚本）
        ↓
慢命分，五优化（Redis性能瓶颈）
```


### Redis缓存问题链路
```
缓锁分，六场景（Redis应用场景）
        ↓
热分本，五解法（热点key问题）
        ↓
穿透崩，三问题（缓存击穿/穿透/雪崩）
        ↓
大拆删，四解法（Big Key问题）
        ↓
慢命分，五优化（Redis性能瓶颈）
        ↓
查大删，五解法（Redis内存溢出）
```

### Redis持久化与内存链路
```
快AOF，两机制（持久化机制）
        ↓
懒定随，三策略（过期删除策略）
        ↓
淘近LFU，八策略（内存淘汰策略）
```


### Redis高可用链路
```
写时复，bgsave（RDB生成原理）
        ↓
快AOF，两机制（持久化机制）
        ↓
同命部，三阶段（主从复制）
        ↓
监故选，三作用（哨兵机制）
        ↓
批一发，三优势（Pipeline）
        ↓
原评调，三用途（Lua脚本）
```

### Redis缓存问题链路
```
缓锁分，六场景（Redis应用场景）
        ↓
热分本，五解法（热点key问题）
        ↓
穿透崩，三问题（缓存击穿/穿透/雪崩）
        ↓
大拆删，四解法（Big Key问题）
        ↓
慢命分，五优化（Redis机器爆了优化）
        ↓
查大删，五解法（Redis内存溢出）
```

### MySQL索引链路
```
选短小，六注意（建索引注意事项）
        ↓
少改重NULL，五不建（不推荐建索引场景）
        ↓
最左前，四原则（最左前缀匹配）
        ↓
叶矮顺，散红黑高（为什么用B+树）
        ↓
深子延，三解法（深度分页）
```

### 复习用法
考前10分钟复习流程：
1. 先看 速记卡片表格（30秒）
2. 再看 知识链路图（1分钟）——顺着链路默念口诀
3. 最后看 总索引 _INDEX.md（30秒）——跨领域联想
